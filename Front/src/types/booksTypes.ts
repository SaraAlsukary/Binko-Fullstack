export type TBooks = {
    id?: number,
    is_accept?: boolean,
    user?: string,
    name?: string,
    image?: string,
    description?: string,
    categories?: [],
    publication_date?: string
}