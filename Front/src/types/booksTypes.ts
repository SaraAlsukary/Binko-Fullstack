export type TBooks = {
    id?: number,
    is_accept?: boolean,
    user?: string,
    name?: string,
    image?: string,
    description?: string,
    category?: number,
    publication_date?: string
}