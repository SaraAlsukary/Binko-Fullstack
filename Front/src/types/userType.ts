export type TUser = {
    id?: number,
    is_accept?: number,
    is_reader?: boolean,
    username?: string,
    age?: number,
    email?: string,
    category?: string,
    discriptions: string | null,
    image: string | null,
    name: string
}